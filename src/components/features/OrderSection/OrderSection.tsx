import ListOrder from '@/components/features/ListOrder/ListOrder';
import { Box } from '@mui/material';

export default function OrderSection() {
    return (
        <Box>
            <ListOrder />
        </Box>
    );
}
